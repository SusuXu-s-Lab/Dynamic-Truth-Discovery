# Dynamic-Truth-Discovery

## Usage
The ```score_calculation.py``` is the file to calculate the aggregated score (confidence score, independence score ,and user score) for social media posts. This program only calculated the score for each data points, without physical constraints and filter scheme.

To run this code:
```bash
python score_calculation.py --input input_file_name.csv --output output_file_name.csv
```
